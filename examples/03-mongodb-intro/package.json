{
  "name": "03-mongodb-intro",
  "version": "1.0.0",
  "type": "module",
  "description": "",
  "main": "index.js",
  "scripts": {
    "prestart-db": "mkdir -p db-data",
    "start-db": "mongod --dbpath ./db-data",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "prettier": "^2.5.1"
  },
  "dependencies": {
    "mongodb": "^4.4.0"
  }
}
